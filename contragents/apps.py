from django.apps import AppConfig
from django.utils.translation import ugettext_lazy as _

class ContragentsConfig(AppConfig):
    name = 'contragents'
    verbose_name = _('Contragents and contacts')
